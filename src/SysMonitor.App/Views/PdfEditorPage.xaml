<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SysMonitor.App.Views.PdfEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource BackgroundBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header Banner -->
        <Border Grid.Row="0" Background="#000000" Padding="24" Margin="-24,-24,-24,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center" Spacing="4">
                    <TextBlock Text="PDF EDITOR" Style="{StaticResource PageHeaderStyle}"
                               Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Left"/>
                    <TextBlock Text="Edit, Annotate &amp; Manage PDF Pages" Style="{StaticResource PageSubtitleStyle}"
                               Foreground="#AAFFFFFF" HorizontalAlignment="Left"/>
                </StackPanel>

                <Image Grid.Column="1" Source="ms-appx:///Assets/Home_Logo.png"
                       Height="80" VerticalAlignment="Center" HorizontalAlignment="Center"
                       Stretch="Uniform"/>
            </Grid>
        </Border>

        <!-- Minimal Content - Just a centered message and button -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                <FontIcon Glyph="&#xEA90;" FontSize="64" Foreground="#808080"/>
                <TextBlock Text="PDF Editor" Style="{StaticResource CardTitleStyle}" HorizontalAlignment="Center"/>
                <TextBlock Text="Click Open to load a PDF file" Style="{StaticResource CardSubtitleStyle}" HorizontalAlignment="Center"/>
                <Button Click="OpenPdf_Click" Style="{StaticResource RedButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8E5;" FontSize="14"/>
                        <TextBlock Text="OPEN PDF"/>
                    </StackPanel>
                </Button>
                <TextBlock x:Name="StatusText" Text="" Foreground="#4CAF50" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
