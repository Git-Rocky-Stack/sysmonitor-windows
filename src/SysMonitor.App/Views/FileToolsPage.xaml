<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SysMonitor.App.Views.FileToolsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:utilities="using:SysMonitor.Core.Services.Utilities"
    Background="{StaticResource BackgroundBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="0,0,0,24" Spacing="16">

            <!-- Page Header Banner -->
            <Border Style="{StaticResource HeaderBannerStyle}">
                <StackPanel HorizontalAlignment="Center" Spacing="8">
                    <FontIcon Glyph="&#xE8B7;" FontSize="48" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="FILE TOOLS" Style="{StaticResource PageHeaderStyle}"
                               Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Compress and extract files" Style="{StaticResource PageSubtitleStyle}"
                               Foreground="#AAFFFFFF" HorizontalAlignment="Center" Margin="0"/>
                </StackPanel>
            </Border>

            <!-- File Selection Section -->
            <StackPanel Style="{StaticResource SectionHeaderWithAccentStyle}">
                <Border Style="{StaticResource AccentBarStyle}" Height="20"/>
                <TextBlock Text="SELECT FILE OR FOLDER" Style="{StaticResource SectionHeaderStyle}" Margin="0"/>
            </StackPanel>

            <Border Style="{StaticResource CardStyle}" Padding="24">
                <Grid RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Selection Buttons -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center">
                        <Button Command="{x:Bind ViewModel.SelectFileCommand}" Style="{StaticResource OutlineButtonStyle}"
                                Width="180">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8A5;" FontSize="16"/>
                                <TextBlock Text="SELECT FILE"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{x:Bind ViewModel.SelectFolderCommand}" Style="{StaticResource OutlineButtonStyle}"
                                Width="180">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8B7;" FontSize="16"/>
                                <TextBlock Text="SELECT FOLDER"/>
                            </StackPanel>
                        </Button>
                        <Button Content="CLEAR" Command="{x:Bind ViewModel.ClearSelectionCommand}"
                                Style="{StaticResource OutlineButtonStyle}"
                                Visibility="{x:Bind ViewModel.HasSelectedFile, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>

                    <!-- Selected File Info -->
                    <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="16"
                            Visibility="{x:Bind ViewModel.HasSelectedFile, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Width="48" Height="48" CornerRadius="8"
                                    Background="{StaticResource CyanAccentBrush}">
                                <FontIcon Glyph="&#xE7C3;" FontSize="24" Foreground="{StaticResource TextPrimaryBrush}"/>
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind ViewModel.SelectedFileName, Mode=OneWay}"
                                           FontWeight="SemiBold" FontSize="16"
                                           Foreground="{StaticResource TextPrimaryBrush}"/>
                                <TextBlock Text="{x:Bind ViewModel.SelectedFilePath, Mode=OneWay}"
                                           Style="{StaticResource CardSubtitleStyle}" FontSize="11"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBlock Text="SIZE" Style="{StaticResource StatLabelStyle}"
                                           HorizontalAlignment="Right"/>
                                <TextBlock Text="{x:Bind ViewModel.FileSize, Mode=OneWay}"
                                           Foreground="{StaticResource CyanAccentBrush}"
                                           FontWeight="Bold" FontSize="18"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- Action Feedback -->
            <Border Background="{StaticResource CardBrush}" CornerRadius="8" Padding="16"
                    Visibility="{x:Bind ViewModel.HasActionStatus, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <FontIcon Grid.Column="0" Glyph="&#xE73E;" FontSize="20">
                        <FontIcon.Foreground>
                            <SolidColorBrush Color="{x:Bind ViewModel.ActionStatusColor, Mode=OneWay}"/>
                        </FontIcon.Foreground>
                    </FontIcon>
                    <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.ActionStatus, Mode=OneWay}"
                               Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"
                               TextWrapping="Wrap"/>
                </Grid>
            </Border>

            <!-- Compression Section -->
            <StackPanel Style="{StaticResource SectionHeaderWithAccentStyle}">
                <Border Style="{StaticResource AccentBarStyle}" Height="20"/>
                <TextBlock Text="COMPRESSION" Style="{StaticResource SectionHeaderStyle}" Margin="0"/>
            </StackPanel>

            <Border Style="{StaticResource CardStyle}" Padding="24">
                <Grid RowSpacing="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Compression Options -->
                    <Grid Grid.Row="0" ColumnSpacing="24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Compress Card -->
                        <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}"
                                CornerRadius="12" Padding="20">
                            <StackPanel Spacing="16">
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <Border Width="48" Height="48" CornerRadius="8"
                                            Background="{StaticResource SuccessBrush}">
                                        <FontIcon Glyph="&#xE81E;" FontSize="24"
                                                  Foreground="{StaticResource TextPrimaryBrush}"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="COMPRESS" FontWeight="Bold" FontSize="16"
                                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                                        <TextBlock Text="Create archive from file/folder"
                                                   Style="{StaticResource CardSubtitleStyle}"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <TextBlock Text="Format:" VerticalAlignment="Center"
                                               Foreground="{StaticResource TextSecondaryBrush}"/>
                                    <ComboBox ItemsSource="{x:Bind ViewModel.CompressionFormats}"
                                              SelectedItem="{x:Bind ViewModel.SelectedCompressionFormat, Mode=TwoWay}"
                                              Width="120"/>
                                </StackPanel>

                                <Button Content="COMPRESS" Command="{x:Bind ViewModel.CompressCommand}"
                                        Style="{StaticResource RedButtonStyle}" HorizontalAlignment="Stretch"
                                        IsEnabled="{x:Bind ViewModel.HasSelectedFile, Mode=OneWay}">
                                    <Button.ContentTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <FontIcon Glyph="&#xE81E;" FontSize="14"/>
                                                <TextBlock Text="COMPRESS"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </Button.ContentTemplate>
                                </Button>
                            </StackPanel>
                        </Border>

                        <!-- Extract Card -->
                        <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}"
                                CornerRadius="12" Padding="20">
                            <StackPanel Spacing="16">
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <Border Width="48" Height="48" CornerRadius="8"
                                            Background="{StaticResource CyanAccentBrush}">
                                        <FontIcon Glyph="&#xE896;" FontSize="24"
                                                  Foreground="{StaticResource TextPrimaryBrush}"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="EXTRACT" FontWeight="Bold" FontSize="16"
                                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                                        <TextBlock Text="Extract files from archive"
                                                   Style="{StaticResource CardSubtitleStyle}"/>
                                    </StackPanel>
                                </StackPanel>

                                <TextBlock Text="Supports: .zip, .gz archives"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="12"/>

                                <Button Content="EXTRACT" Command="{x:Bind ViewModel.DecompressCommand}"
                                        Style="{StaticResource OutlineButtonStyle}" HorizontalAlignment="Stretch"
                                        IsEnabled="{x:Bind ViewModel.HasSelectedFile, Mode=OneWay}">
                                    <Button.ContentTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <FontIcon Glyph="&#xE896;" FontSize="14"/>
                                                <TextBlock Text="EXTRACT"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </Button.ContentTemplate>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Processing Indicator -->
                    <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="16"
                            Visibility="{x:Bind ViewModel.IsProcessing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal" Spacing="16" HorizontalAlignment="Center">
                            <ProgressRing IsActive="True" Width="24" Height="24"/>
                            <TextBlock Text="{x:Bind ViewModel.ProcessingStatus, Mode=OneWay}"
                                       VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Result Section -->
            <Border Style="{StaticResource CardStyle}" Padding="24"
                    Visibility="{x:Bind ViewModel.HasResult, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE73E;" FontSize="28" Foreground="{StaticResource SuccessBrush}"/>
                        <TextBlock Text="OPERATION COMPLETE" FontWeight="Bold" FontSize="18"
                                   VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>

                    <Grid ColumnSpacing="24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}"
                                CornerRadius="8" Padding="16">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="ORIGINAL SIZE" Style="{StaticResource StatLabelStyle}"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="{x:Bind ViewModel.ResultOriginalSize, Mode=OneWay}"
                                           FontWeight="Bold" FontSize="20"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}"
                                CornerRadius="8" Padding="16">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="NEW SIZE" Style="{StaticResource StatLabelStyle}"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="{x:Bind ViewModel.ResultNewSize, Mode=OneWay}"
                                           FontWeight="Bold" FontSize="20"
                                           Foreground="{StaticResource SuccessBrush}"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}"
                                CornerRadius="8" Padding="16">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="SPACE SAVED" Style="{StaticResource StatLabelStyle}"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="{x:Bind ViewModel.ResultSaved, Mode=OneWay}"
                                           FontWeight="Bold" FontSize="20"
                                           Foreground="{StaticResource CyanAccentBrush}"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Button Content="OPEN OUTPUT FOLDER" Command="{x:Bind ViewModel.OpenOutputFolderCommand}"
                            Style="{StaticResource OutlineButtonStyle}" HorizontalAlignment="Center">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE838;" FontSize="14"/>
                                    <TextBlock Text="OPEN OUTPUT FOLDER"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Tips Section -->
            <StackPanel Style="{StaticResource SectionHeaderWithAccentStyle}">
                <Border Style="{StaticResource AccentBarStyle}" Height="20"/>
                <TextBlock Text="COMPRESSION TIPS" Style="{StaticResource SectionHeaderStyle}" Margin="0"/>
            </StackPanel>

            <Border Style="{StaticResource CardStyle}" Padding="20">
                <StackPanel Spacing="12">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE8B7;" Foreground="{StaticResource CyanAccentBrush}" FontSize="18"/>
                        <TextBlock Text="ZIP format is widely compatible and supports multiple files/folders"
                                   Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE81E;" Foreground="{StaticResource CyanAccentBrush}" FontSize="18"/>
                        <TextBlock Text="GZIP provides higher compression for single files"
                                   Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE946;" Foreground="{StaticResource CyanAccentBrush}" FontSize="18"/>
                        <TextBlock Text="Already compressed files (videos, images) won't compress much further"
                                   Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Footer -->
            <StackPanel HorizontalAlignment="Center" Spacing="8" Margin="0,16,0,0">
                <Image Source="ms-appx:///Assets/Header_Main1.png"
                       Height="16" HorizontalAlignment="Center"
                       Stretch="Uniform" Opacity="0.7"/>
                <TextBlock Text="Powered by Strategia-X Technology"
                           HorizontalAlignment="Center"
                           Foreground="{StaticResource PrimaryBrush}"
                           FontSize="12"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
